spring:
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: jdbc:mysql://${DB_ENDPOINT}:3306/SABY
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
  data:
    web:
      pageable:
        max-page-size: 100
        default-page-size: 20

auth:
  jwt:
    secret: secret
    exp:
      access: 1000000
